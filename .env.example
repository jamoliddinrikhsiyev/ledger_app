# Master gate for every outbound service call.
#
# Left unset (or anything other than the exact string "true") the app makes no
# external requests at all — it runs fully offline against local SQLite.
# Individual services also carry their own `enabled` flag in
# src/services/config.ts; both gates must pass before a request is sent.
VITE_SERVICES_ENABLED=false
